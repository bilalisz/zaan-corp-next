const { makeStyles } = require("@mui/styles");

export const useMainCntStyle = makeStyles((theme) => ({
  mainCnt: {
    padding: "20px 10px",
    width: "90%",
    height: "100vh",
    backgroundColor: "#f7f7f7",
  },
}));
